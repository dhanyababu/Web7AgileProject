@model List<ProjectAgileWeb7.Models.HotelUser>
@{
    ViewData["Title"] = "Favorites";
}


<h1>Favorites</h1>

@if (@Model.Count() == 0)
{
    <p>No favorites </p>
}
else
{
    @foreach (var item in @Model)
    {
        @item.Hotel.Name

        <br />
        <br />
    }
}
